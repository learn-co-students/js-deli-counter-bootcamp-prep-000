katzDeliLine = [];

function takeANumber(currentLine, name){
  var array = [];
  var i = 1;
  for (let i = 1; i < array.length; i++){
 return array.push(`Welcome, ${name}. You are number ${i} in line.`);
}
return array;
}