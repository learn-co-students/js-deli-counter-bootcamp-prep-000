function takeANumber(katzDeliLine,name) {
  katzDeliLine.push(name);
  return 'Welcome, ' + katzDeliLine + '. You are number ' + katzDeliLine.length + ' in line.'
  }
function takeANumber(otherDeli,name) {
  otherDeli.push(name);
  return 'Welcome, ' + otherDeli[] + '. You are number ' + otherDeli.length + ' in line.'
}
