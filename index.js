var katzDeli = []

function takeANumber(katzDeliLine, name) {
  var n = katzDeliLine + 1
  var greeting = "Welcome, " + name + ". You are number " + n + " in line."
  array.push(name)
  return greeting
}