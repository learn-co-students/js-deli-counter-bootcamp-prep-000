var katzDeli = [];

function takeANumber(katzDeliLine, name) {
  var lineHash = {name: katzDeliLine};
  for (i = 0; i < name; i++) {
  `Welcome, ${name}. You are number ${katzDeliLine + 1} in line.`;
    
  }
}