var katzDeli[];

function takeANumber(katzDeliLine, name); {
  katzDeliLine.push(name);
  position = katzDeliLine.index(name);
  console.log('Welcome, ' + name + 'you are ' + position + 'in line.');
  return (name, position);
}
